export const body = (position, rotation) => ({ position, rotation })

export const velocity = (linear, angular) => ({ linear, angular })

export const gravity = () => ({})

export const block = (type) => ({ type })
